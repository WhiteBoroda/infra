## Copy this file to values-secrets.yaml (do not commit the new file)
## and replace the placeholder values with real credentials.
odoo:
  database:
    password: "dev_db_password"
  adminPassword: "dev_admin_password"

postgresql:
  auth:
    password: "dev_db_password"
